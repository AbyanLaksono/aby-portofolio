{
  "name": "aby-portofolio",
  "private": true,
  "version": "0.0.0",

  "homepage": "https://AbyanLaksono.github.io/aby-portofolio",

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },

  "dependencies": {
  },

  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}